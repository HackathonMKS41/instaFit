var helpers = require('../config/helpers');
var express = require('express');
var router = express.Router();

// templates:
// router.get('/endpoint', helpers.helperFunction);
// router.post('/endpoint', helpers.helperFunction);

module.exports = router;